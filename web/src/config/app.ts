/**
 * App-wide constants. Kept intentionally tiny in chunk 0.1 — grows as
 * features land in later chunks.
 */
export const APP_NAME = "Partner Operating System" as const;
export const APP_DESCRIPTION =
  "Multi-tenant business operating system for COD / Shopify merchants." as const;
